{
  "name": "Auth Theme Switcher",
  "manifest_version": 2,
  "version": "0.1",
  "description": "Keeps the web page color theme settings in sync with system color theme.",
  "permissions": [
    "background"
  ],
  "content_scripts": [
    {
      "matches": ["https://*.slack.com/*"],
      "js": ["slack.js"]
    }
  ]
}
